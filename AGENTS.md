# Local REAPER install

After completing changes to runtime Lua or icon fonts, run `bash tools/install.sh`
to install the latest code into `/Users/ferg/Desktop/REAPER/Scripts/Fergler/Advanced Toolbars`.
Report any install failure. The installer preserves the destination's `User/` settings.
Use `bash tools/install.sh --watch` for automatic syncing during manual development.
